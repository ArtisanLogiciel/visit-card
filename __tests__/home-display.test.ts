// import '@testing-library/jest-dom';
// import { render, screen } from '@testing-library/react';
// import { Home } from 'lucide';
// import { BrowserRouter } from 'react-router-dom';

// describe('Home Component', () => {
//   test('it should render the Home component', () => {
//     const { getByText } = render(
//       <BrowserRouter>
//         <Home />
//       </BrowserRouter>
//     );

//     // Vérifie que le titre "Cartes de visites" est présent
//     expect(screen.getByText('Cartes de visites')).toBeInTheDocument();

//     // Vérifie que les boutons "S'authentifier" et "S'inscrire" sont présents
//     expect(screen.getByText("S'authentifier")).toBeInTheDocument();
//     expect(screen.getByText("S'inscrire")).toBeInTheDocument();
//   });
// });
